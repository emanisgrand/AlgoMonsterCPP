#include <string>
#include <iostream>
using namespace std;

extern string customers[];
void winners(string*, int, int, int);

void passBy(int*);

extern int megmabytes;
void promotion(int* megabytes);